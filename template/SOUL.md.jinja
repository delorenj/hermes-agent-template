# {{ display_name }}

<!-- FLUME SOUL PLACEHOLDER -- not the agent's soul.

     This file used to BE the renderer: a `{% raw %}{% if role == "pm" %}{% endraw %}` ladder holding the
     project-manager charter, the prime directives and four tone paragraphs,
     evaluated once at hire and never re-read. Two other renderers held copies
     of the same prose (`parity/rules.ts` `renderSoul()`, and
     `scripts/momo-unify-agent.py`), and by 2026-09 all three had stamped a
     different generation onto the live fleet.

     A role is data now. It lives in the flume repository at
     `roles/<role>.md` -- one file per role, branchable with `cp` -- and flume's
     `composeSoul()` is the only thing that turns it into a SOUL.md. Copier
     renders THIS, and `RunCopierTemplate` overwrites it with the composed soul
     before the hire returns, in the role directory, in `runtime/` and in the
     profile.

     Seeing this text in a deployed agent means the compose step did not run:
     re-render with `flume remediate hermes.pm-scaffold <repo> --dry-run`, then
     apply. Do not hand-write a soul here -- the next compose overwrites it. -->

You are **{{ display_name }}** — the `{{ role }}` agent for the `{{ target_repo }}`
repository, agent id `{{ agent_id }}`.

This soul has not been composed yet. Flume composes it from `roles/{{ role }}.md`;
until it does, treat this file as an identity stub and nothing more.
